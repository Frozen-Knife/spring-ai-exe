# 架构师基础（你都得会！不会等通知，会了应该的）

## 一、进程、线程、虚拟线程的区别。（融360）

> 进程：就是操作系统中的一个独立单元。进程之间是相互隔离的……
>
> 线程：绑定到某一个进程内部的，是进程中的一个最小的执行单位。这哥们是CPU直接调度的……
>
> 虚拟线程：
>
> * 在Java中，如果存在IO密集的任务时，线程个数不太好空格，如果用了虚拟线程，就不需要关注个数了。
>   * 正常线程占用的内存资源在MB级别。虚拟线程占用的资源在KB资源。
>   * 正常线程由CPU直接调度，存在上下文切换带来的时间成本，而且存在用户态跟内核态切换的时间成本。 虚拟线程不是由CPU直接调度的，所以不存在所谓的上下文切换成本，而且没有所谓的挂起跟唤醒去切换状态的时间成本。
> * 虚拟线程无法解决CPU密集的任务，因为你CPU资源就那么多，不可能处理性能变高的。

如果你会基本的线程池操作，那虚拟线程就是白给的：

https://www.mashibing.com/live/2809?r=2562&courseVersionId=3758

## 二、重写，重载。（小牛筑讯）

> 重写：（Override）
>
> * 重写是子类重写父类、父接口中的方法，要求方法名，方法参数等必须跟父类一模一样。
> * 但是返回结构需要适配父类方法，访问修饰符不能小于父类的方法。
>
> 重载：（OverLoad）
>
> * 重载是在一个类里，方法名一致，方法参数不同。
> * 返回结果，方法修饰符无所谓。

## 三、String，Buffer，Builder（百度外包）

> String：
>
> * 不可变长度的，你即便 + 之类的，他也是构建一个全新的字符串，对内存占用比较大。
> * 线程安全的
>
> StringBuffer：
>
> * 长度可变的，底层采用char[]去实现字符串的拼接，可以规避String那种每次创建新的字符串。
> * 线程安全的，加了synchronized，如果涉及到成员变量的拼接，记得用StringBuffer
>
> StringBuilder：
>
> * 长度可变的，底层采用char[]去实现字符串的拼接，可以规避String那种每次创建新的字符串。
> * 线程不安全的，使用他需要注意，需要在局部变量自己玩的时候，记得用StringBuilder
>
> **Ps：其中在JDK1.8之前，Buffer还是Builder采用的是char[]，但是在JDK1.9之后，优化为了byte[]。**
>
> **目的就是节约空间，正常char字符一个需要占用2字节，而一些英文，标点符号，其实1字节就够用，理论上，最多节约了一半的空间。**

## 四、Autowired和Resource注解（信迪科技）

> @Autowired：
>
> * Spring提供的注解。
> * 默认类型注入，类型找到一个，直接注入，找不到，或者找到2个以上，直接报错。
> * 如果找到多个要匹配，可以追加@Qualifier注解，指定名称。
>
> @Resource：
>
> * Java规范提供的。
> * 默认基于属性名注入，如果名字找不到，会再根据类型去找，类型找不到匹配的，也是凉凉。

## 五、Java中常见的集合。（小牛筑讯）

> Java中主要就是单列集合跟双列集合。
>
> 单列：
>
> * List：存取有序，允许重复。底层有数组ArrayList和链表LinkedList实现。
> * Set：存取无序，不允许重复。底层是HashMap的key。
>
> 双列：
>
> * Map：存储双列，key不允许重复，value无所谓。  **（建议扩展Hash）**
> * Map这可以展开去聊一些底层实现，存储结构，线程安全问题等等。

## 六、线程安全的集合。（北京小米）

> 从三个方向来聊
>
> **比较传统的线程安全集合：** Vector，Hashtable，Collections.synch...这种，完全基于synchronized，锁全局，性能贼慢，基本不用。
>
> **CopyOnWrite系列：** CopyOnWriteArrayList这种，他是利用空间换时间，来保证查询的时候不会阻塞，但是在写入的时候，依赖是基于lock锁保证线程安全。
>
> **Concurrent系列：** 这个优化的程度会比较大，一般会结合CAS  + synchronized去保证写操作的线程安全，同时查询依然不会阻塞。 **建议拓展（ConcurrentHashMap）**

## 七、对象为啥要有hashCode方法。（数智边界科技）

> Java中对象的hashCode方法就是提供给HashSet，HashMap这种集合，做一些hash寻址之类的操作。也可以配置eq做数据的比对。
>
> **hashCode啥前生成**
>
> 如果hashCode方法重写了，那你每次调用的时候，他才会生成对应的hash值。
>
> 如果hashCode没重写，他固定就是你的全路径跟地址的信息，这个是不会变的。
>
> **equals重写了，必须重写hashCode么？**
>
> * **仅在郑老师这，仅代表个人观点，不是。** 因为你知道这个对象的用途是什么的时候，完全可以根据你的用途去考虑你的hashCode跟equals合适重写，怎么重写。。。
> * 如果是针对HashMap用，那最好是都重写，但是也不是绝对的。更多的是满足Java规范~~~

## 八、Session和Cookie。（融360）

> Cookie集合Session一般是实现会话的。
>
> Cookie：
>
> * 存储在客户端的。
> * 存储有大小的限制。
> * 不太安全，毕竟可以在客户端直接查看到。
>
> Session：
>
> * 存储在服务端。
> * 存储大小理论上没限制，跟你的JVM内存走。
> * 安全，毕竟在服务端嘛。
>
> **扩：毕竟是比较传统的项目做会话控制的方式，现在用的不多，可以聊一些框架SpringSecurity，JWT……**

## 九、HashMap，为啥key可以为null。（融360）

> 一般这个问题最好是结合ConcurrentHashMap去聊。
>
> 因为ConcurrentHashMap中，key-value不允许为null。而HashMap中没有这个限制。
>
> 因为ConcurrentHashMap一般是作为成员变量，多个线程都会操作，如果一个线程null存储特殊的标识，其他线程不知道。。。。。。
>
> 而HashMap是线程不安全的，必然是局部使用，就一个线程自己玩，那你自己玩，存个null，代表什么特殊信息，完全没问题。。


## 十、线程安全问题怎么解决（鸿图云智科技、小牛筑讯、数智边界科技……）

> **多个线程 同时修改 临界资源** 导致的线程安全问题。
>
> 多个线程：就一个线程玩，自然没有线程安全问题，一些资源不是共享的，扔ThreadLocal自己玩。
>
> 同时修改：
>
> * 不让他同时修改，直接加锁！！CAS，sync，lock……   **（扩展点一定在这，其次volatile也可以聊）**
> * 不修改，final修改的常量。
>
> 临界资源：不去做写操作，或者自己玩自己的局部资源，直接从根源解决问题。



## 十一、Spring声明式事务失效情况。（天翼通讯）

> 失效情况很多：
>
> * 方法基于private修改，导致AOP无法增强这个方法。
> * 类没有实现的接口，并且基于final修改，导致AOP无法创建代理对象。
> * 因为声明式事务默认搞RuntimeException，如果玩其他的自定义异常，也会失效。
> * 异常被你自己try-catch了，没往上抛，导致异常增强不走。
> * 在方法内部基于this.方式，或者方法内部搞多线程，都没走Spring增强后的代理对象。
> * 确保数据源的目标数据库支持事务！！

## 十二、事务传播特性（天翼通讯）

> 存在声明式事务增强的方法直接进行相互调用，存在事务传播特性的问题：
>
> * **REQUIRED：必然在一个事务里！走外层事务。（默认的）**
> * **SUPPORT：随缘，有事务就加入外层事务，没事务，那就不走事务！**
> * **REQUIRED_NEW：不管怎么玩，都创建自己的新事务。**
> * **NOT_SUPPORT：不走事务！**
> * NESTED：嵌套事务的概念，自己回滚不影响外部，但是外部回滚，内部也回滚。
> * NEVER：不走事务，你有事务，扔异常。
> * MANDATORY：走事务，没事务，扔异常。
>
> **就用默认的！**

## 十三、AOP实现的原理（天翼通讯）

> 可以从代理模式去聊。无非是
>
> JDK动态代理：需要基于接口实现。被代理类必须要有实现的接口
>
> Cglib动态代理：继续基于继承实现，被代理类不能被final修饰。
>
> JDK跟Cglib几乎大差不差，虽然Cglib基于字节增强去玩，但是JDK代理也在优化。
>
> 想再拓展，就聊Spring源码，聊BeanPostProcessor……

## 十四、Spring中涉及的设计模式。（字节测开）

> 单例、工厂、代理、观察者、模板、原型、责任链……
>
> 一个要求，聊到的，要知道大概再哪个位置用到
>
> 点到的，必须知道在哪用的。
>
> **如果单独问设计模式，最好聊自己项目中真实落地的，别聊框架涉及的！**

## 十五、类加载器都有什么（鸿翔一心堂）

> Java中自带了三，咱们自己也可以主动去实现，一般会说四种。
>
> * BootstrapClassLoader：默认rt.jar以及一些其他的核心包
> * ExtClassLoader：默认ext目录
> * AppClassLoader：默认classpath下
> * CustomClassLoader：指哪打哪
>
> **双亲委派：**
>
> 本质就是在加载.class文件时，会向上委托的方式，优先让最高层的ClassLoader去加载，如果加载不到，再往下去委派。
>
> 代码实现本质就是基于递归走的。
>
> 第一次走是从Custom - App - Ext - Bs，这次走的过程是加载过没？加载过，直接用。
>
> 第二次是从Bs - Ext - App - Custom，这次是去加载，根据每个ClassLoader负责的位置，去加载。
>
> 核心目的是为了规避核心类被篡改。
>
> ![image.png](https://fynotefile.oss-cn-zhangjiakou.aliyuncs.com/fynote/fyfile/2746/1755772414067/1edc2dc3e44e49f495d9cc3299bd5e72.png)

## 十六、JVM内存结构（百度外包、宏卓科技）

> 一定要区分好，Java内存模式问的是JMM奥~别聊错了！
>
> 从俩维度聊
>
> * 线程私有：虚拟机栈，本地方法栈，程序计数器
> * 线程共享：堆，方法区（方法区是JVM虚拟机的规范，一般实现是元空间）
>
> ![image.png](https://fynotefile.oss-cn-zhangjiakou.aliyuncs.com/fynote/fyfile/2746/1755772414067/5851f4a14f9a475bafd4080eaf81850f.png)

## 十七、垃圾回收算法（小牛筑讯）

> 常见的三个：
>
> * **复制清除：** 理论上将内存分为两片区域，只在一片区分存放对象，一片满了，保留可用对应复制到另一片，然后清空当前片内存。  **空间浪费、回收速度快**
> * 标记清除： 根据不同的算法，比如三色标记对可用对象进行标记，将没标记到的，直接干掉。 **空间没问题，回收速度可以，但是有内存碎片问题。**
> * 标记压缩、整理： 根据不同的算法，比如三色标记对可用对象进行标记，将没标记到的，直接干掉。然后整理空间，避免碎片化问题。    **空间ok，速度稍慢，没有碎片化问题。**

## 十八、常见垃圾回收器（宏卓科技）

> 从古老的，聊到新的。
>
> Serial New、Serial Old：非常远古的垃圾回收器，单线程回收，速度嘎嘎慢。
>
> Parallel New、Parallel Old：JDK1.8默认的垃圾回收器，多线程回收，速度稍快，有STW。
>
> ParNew、CMS：CMS是并行的垃圾回收器，除了初始标记、重新标记位置需要STW之外，回收的时候是并行的，没有STW。
>
> G1：分区回收，把整个堆分成上千个区域，每次就回收垃圾最多的位置，STW时间短。
>
> ZGC：毫秒级别的回收，不需要做JVM调优相关的操作。
>
> ~~Shenandoah：速度也很快，毫秒级别的。~~

**程序计数器记录的是当前指令还是下一条指令。JVM实现的很多，可能根据不同的虚拟机，走的方式不一样。（暂时没看，但是个人认为是下一条，因为指令应该是跟着类似地址的形式走的，要想迭代器类似的方式走）**

**高版本没有JRE了！？高版本的JDK结构有一些小优化。**
